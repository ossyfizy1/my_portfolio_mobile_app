import 'package:flutter/material.dart';

// creates a new instance of NavigatorState ---> GlobalKey<NavigatorState>() and use it anywhere (without context needed)
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
